import QtQuick 1.1

Item {
    // providing dummy translations that can be used on settings layout files
    function qsTrIdString() {
        //% "Transfers"
        QT_TRID_NOOP("settings_transferui-transferui")
    }
}
